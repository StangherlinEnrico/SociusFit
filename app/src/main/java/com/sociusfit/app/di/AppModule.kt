package com.sociusfit.app.di

import org.koin.dsl.module

/**
 * App Module - Koin
 *
 * Contiene dipendenze specifiche del modulo :app.
 * Al momento vuoto, ma può contenere in futuro:
 * - App-level managers
 * - App-wide state holders
 * - Analytics trackers
 */
val appModule = module {
    // Nessuna dipendenza specifica per ora
    // Le dipendenze sono nei core modules e feature modules
}